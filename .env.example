REACT_APP_RECONSTRUCTION_API_URL=http://127.0.0.1:8787

# Reconstruction worker
PORT=8787
HOST=127.0.0.1
PUBLIC_BASE_URL=http://127.0.0.1:8787
ALLOWED_ORIGIN=http://localhost:3000
COLMAP_PATH=colmap
# Start on CPU. Set this to 1 only when the worker has a CUDA-enabled NVIDIA GPU.
COLMAP_USE_GPU=0
FFMPEG_PATH=ffmpeg
